// the method below selects the element with the id ofhead
let id = document.getElementById('body');
// the code below selects the first p element inside the first div
let q = document.querySelector('pre div.klipse-snippet.klipse-eval-clojure div');
id.q.innerText = '"our new text"';